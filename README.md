# rag-eval-harness

Evaluate whether a local RAG API retrieves the expected documents for a set of test questions.

## Setup

```bash
uv sync
```

## CLI

```bash
uv run rag-eval check
```

## Environment

No environment variables are required for the current scaffold.

Future API evaluation settings are documented in `.env.example`.
